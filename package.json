{
  "name": "github-file-storage",
  "version": "1.0.0",
  "description": "File upload/download application storing files in GitHub repository",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server -p 8080 -c-1",
    "build": "echo 'No build required - static HTML/CSS/JS'",
    "test": "npx http-server -p 8080 -c-1"
  },
  "keywords": [
    "github",
    "file-storage",
    "pwa",
    "static",
    "github-pages"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=16"
  }
}